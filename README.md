# Webscrapper


###To setup
```apt update && apt upgrade
   apt install git
   apt install python3```
###To install
   `git clone https://github.com/Enthernetcode/Webscrapper`
###To Run
  `python srch.py`
